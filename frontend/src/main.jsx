import React,{useState}from'react';import{createRoot}from'react-dom/client';const API=import.meta.env.VITE_API_URL||'http://127.0.0.1:8000';function App(){const[t,setT]=useState(localStorage.token||'');const[f,setF]=useState({username:'',password:''});const[tab,setTab]=useState('login');const[q,setQ]=useState('');const[a,setA]=useState('');async function auth(){let r=await fetch(API+'/auth/'+tab,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)}),x=await r.json();if(!r.ok)return setA(x.detail||'Failed');if(tab==='login'){localStorage.token=x.access_token;setT(x.access_token)}else setA('Registered. Login now.')}async function chat(){let r=await fetch(API+'/chat',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+t},body:JSON.stringify({message:q})}),x=await r.json();setA(x.reply||x.detail||'No response')}if(!t)return <main><h1>AI Customer Support</h1><button onClick={()=>setTab('login')}>Login</button><button onClick={()=>setTab('register')}>Register</button><input placeholder="Username" onChange={e=>setF({...f,username:e.target.value})}/><input type="password" placeholder="Password" onChange={e=>setF({...f,password:e.target.value})}/><button onClick={auth}>{tab}</button><p>{a}</p></main>;return <main><h1>AI Customer Support</h1><p>Authenticated support assistant</p><input value={q} placeholder="Ask about refund, password, shipping..." onChange={e=>setQ(e.target.value)}/><button onClick={chat}>Ask AI</button><article>{a}</article><button onClick={()=>{localStorage.clear();setT('')}}>Logout</button></main>}createRoot(document.getElementById('root')).render(<App/>);